PART 1

The program is implemented using pyspark and is written in Jupiter notebook. The program requires python 3.7 or below as pyspark is not supported in python3.8

The program assumes both the datafiles and python notebook file is on the same folder and the name of the file is hardcoded to the code.

PART-2

Part2 of the assignment is created using scala. To run the code you have to add the twitter access credentials in the follwing order 
1)consumerKey
2)consumerSecret 
3)accessToken
4)accessTokenSecret

The input paramaters are specied in (when using intelliJ to run the project)
Run>Edit Configurations> Program argument